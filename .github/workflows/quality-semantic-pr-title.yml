name: Quality Check - PR Title Validation

on:
  pull_request:
    types: [opened, edited, synchronize, reopened, ready_for_review]
  merge_group:
    types: [checks_requested]

permissions: {}

concurrency:
  group: semantic-pr-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:
  # Org ruleset (checks-turbo-themes):
  # semantic-title / 📝 Validate PR Title
  # The reusable no-ops at step level on merge_group (title was already
  # validated on the pull_request event) so the required check still reports.
  semantic-title:
    # yamllint disable-line rule:line-length
    uses: lgtm-hq/lgtm-ci/.github/workflows/reusable-semantic-pr-title.yml@f0e999fd9934a43c8d1d6a1c68ee2d1a8a978f36 # v0.69.8
    with:
      job-name: '📝 Validate PR Title'
      types: |
        chore
        ci
        docs
        feat
        fix
        perf
        refactor
        revert
        style
        test
      require-scope: false
      tooling-ref: 'f0e999fd9934a43c8d1d6a1c68ee2d1a8a978f36' # v0.69.8
      egress-policy: block
      egress-preset: github-tooling
    permissions:
      contents: read
      # post-failure-comment (default: true) upserts failure comments on the PR
      pull-requests: write
