---
name: Reusable - Build & Package
# Reusable workflow for building TypeScript and Jekyll site.
# Uploads build artifacts for use in downstream workflows.

on:
  workflow_call:
    inputs:
      node-version:
        description: Node.js version to use
        type: string
        required: false
        default: '22'
      ruby-version:
        description: Ruby version to use
        type: string
        required: false
        default: '3.3'

permissions:
  contents: read
  actions: write

jobs:
  build:
    name: 🏗️ Build Artifacts
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@20cf305ff2072d973412fa9b1e3a4f227bda3c76 # v2.14.0
        with:
          egress-policy: block
          allowed-endpoints: >
            github.com:443 api.github.com:443 uploads.github.com:443
            pipelines.actions.githubusercontent.com:443
            objects.githubusercontent.com:443 codeload.github.com:443
            github-releases.githubusercontent.com:443
            release-assets.githubusercontent.com:443 registry.npmjs.org:443
            npmjs.org:443 bun.sh:443 rubygems.org:443 api.rubygems.org:443
            index.rubygems.org:443 bundler.rubygems.org:443
            rubygems.global.ssl.fastly.net:443 cache.ruby-lang.org:443 pypi.org:443
            files.pythonhosted.org:443

      - name: Checkout
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: Setup environment
        uses: ./.github/actions/setup-env
        with:
          node-version: ${{ inputs.node-version }}
          ruby-version: ${{ inputs.ruby-version }}

      - name: TypeScript build
        run: bun run build

      - name: Build CSS Themes
        run: bun run build:themes

      - name: Generate tokens (css/python/swift)
        run: bun run build:tokens:all

      - name: Verify generated tokens are committed
        shell: bash
        run: |
          git status --porcelain
          git diff --quiet --exit-code

      - name: Jekyll build
        run: bun run build:ci:jekyll

      - name: Upload JS dist artifact
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: js-dist
          path: dist/
          overwrite: true

      - name: Upload built site artifact
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: site-build
          path: _site/
          overwrite: true
